# Libro de clases — Frontend (Angular)

Frontend único del proyecto: **Angular 19**, patrón **Container / Presentational**.

## Desarrollo

1. Microservicios en Docker (`H/Contenedores`) o locales en **8081–8086**.
2. En esta carpeta:

```powershell
npm.cmd install
npm.cmd start
```

Abre **http://127.0.0.1:4200/** (el proxy está en `proxy.conf.json`).

## Admin por defecto (Docker)

- Email: `admin@boh.cl`
- Contraseña: `Admin123!`

Las cuentas de alumno, apoderado y profesor solo las crea el administrador en **Administración → Crear cuenta**.
